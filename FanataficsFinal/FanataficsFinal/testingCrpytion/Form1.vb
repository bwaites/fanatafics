Imports BusinessObjects
Public Class Form1

    'Private Sub btnEncrypt_Click(sender As System.Object, e As System.EventArgs) Handles btnEncrypt.Click
    '    Dim normalText As String
    '    normalText = txtDecrypt.Text
    '    txtEncrypt.Text = pEncryption.EncryptQueryString(normalText)

    'End Sub

    'Private Sub btnDecrypt_Click(sender As System.Object, e As System.EventArgs) Handles btnDecrypt.Click
    '    Dim encryptedText As String
    '    encryptedText = txtEncrypt.Text
    '    txtDecrypt.Text = pEncryption.DecryptQueryString(encryptedText)
    'End Sub
End Class
